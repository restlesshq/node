// Replaced at build time (tsup) and test time (vitest) with the literal
// version string from package.json. See `define` in tsup.config.ts /
// vitest.config.ts.
declare const __SDK_VERSION__: string;
